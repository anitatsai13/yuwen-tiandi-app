# 國文語文天地 製作方法

> 線上版：<https://anitatsai13.github.io/yuwen-tiandi-app/>
> 單一自包含 HTML（約 3.6MB——含大量 base64 內嵌圖片），無框架、無 build step。

## 檔案結構

```
yuwen-tiandi-app/
├── index.html          ← App 本體（GitHub Pages 首頁）
└── 國文語文天地.html    ← 同內容副本（與 index.html 逐位元相同，方便本機直接點開）
```

兩檔必須保持一致（改版後複製並用檔案雜湊比對）。

## 內容架構：六大單元

`const UNITS={ b1:標點符號, wz:文字構造, zt:字體演變, xc:修辭, yc:語文常識, wx:文學常識 }`

- 每單元＝一個學習頁籤：知識卡、表格（如外來語表、柬帖用語表、借代詞總表、韻文演變彩色時間軸）、CSS 示意圖（如信封格式直接用 CSS 畫）。
- 有題庫的單元掛測驗：`qs` 指向題庫常數（`Q_B1`／`Q_WZ`／`Q_ZT`…），格式 `{q,opts,ans,sec}`。

## 圖片機制（本 App 最特別之處）

- 所有圖片以 base64 存在 `IM={key:dataURI}` 對照表，**不放外部圖檔**。
- 內文／題目用 `⟦imageN⟧` 標記引用圖片，渲染時 `rep()` 置換成 `<img>`：
  - 題幹 → 大圖 `class="qi"`；選項與詳解 → 行內圖 `class="gi"`
  - 圖 key 帶單元前綴（`UNITS[unit].pfx`，如 `z:image3`）避免跨單元衝突
- 好處：字體演變、文字構造這類必須看圖的內容可完整呈現，仍維持單檔離線可用。

## 測驗與其他

- 四選一即時判定＋詳解；`□`（考空格的字）自動高亮。
- `CMP`（比較表）、`MARKS`（標點規則）等常數支撐各單元卡片。
- 原生 DOM 渲染，單元切換整頁重繪。

## 維護流程

1. 改 `index.html` → 複製成 `國文語文天地.html`（hash 比對一致）
2. commit → push → GitHub Pages 自動部署
3. 檔案大（3.6MB）主因是內嵌圖片——新增圖片前先壓縮，避免超過瀏覽器與 Pages 的舒適範圍
